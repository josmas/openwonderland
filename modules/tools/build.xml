<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="dist" name="wonderland-modules-tools">  
     <!-- current directory needed in all scripts -->
    <dirname property="current.dir" file="${ant.file.wonderland-modules-tools}"/>
    
    <!-- all projects -->
    <fileset dir="${current.dir}" id="all.projects">
        <include name="darkstar/build.xml"/>
        <include name="voicebridge/build.xml"/>
        <include name="server-manager/build.xml"/>
        <include name="audio-manager/build.xml"/>
        <include name="sharedstate/build.xml"/>
        <include name="artimport/build.xml"/>
        <include name="orb/build.xml"/>
        <include name="coneofsilence/build.xml"/>
        <include name="phone/build.xml"/>
        <include name="audiorecorder/build.xml"/>
        <include name="artimport/build.xml"/>
        <include name="content-repository/build.xml"/>
        <include name="webdav/build.xml"/>
        <include name="snapshot-manager/build.xml"/>
        <include name="asset-meter/build.xml"/>
    </fileset>
    
    <fileset dir="${current.dir}" id="presence.manager">
        <include name="presence-manager/build.xml"/>
    </fileset>

    <!-- build and package all subprojects -->
    <target name="dist">
        <build-subprojects fileset="presence.manager" target="dist"/>
        <build-subprojects fileset="all.projects" target="dist"/>
    </target>
    
    <!-- clean all sub-projects -->
    <target name="clean">
        <!-- clean sub-projects -->
        <build-subprojects fileset="all.projects" target="clean"/>       
    </target>
    
    <!-- macro to iterate across all subprojects -->
    <macrodef name="build-subprojects">
        <!-- the reference id of the fileset to iterate over -->
        <attribute name="fileset"/>
        
        <!-- the build target to run -->
        <attribute name="target"/>
        <sequential>
            <subant target="@{target}" inheritall="false">
                <fileset refid="@{fileset}"/>
            </subant>
        </sequential>
    </macrodef>
</project>
