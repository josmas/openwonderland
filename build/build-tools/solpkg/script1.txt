#!/bin/sh
PATH=/usr/bin:/bin

WHO=`/usr/xpg4/bin/id -n -u`
if [ ! "$WHO" = "root" ]
then
    echo "ERROR: You need to run this script as root (to add packages using 'pkgadd')"
    exit 1
fi

more <<"EOF"

