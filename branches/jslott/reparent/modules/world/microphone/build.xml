<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." default="dist" name="microphone">
     <!-- current directory needed in all scripts -->
    <dirname property="current.dir" file="${ant.file.microphone}"/>
    <property name="modules.dir" location="${current.dir}/../.."/>
        
    <property name="module.src" value="org/jdesktop/wonderland/modules/microphone"/>
    <property name="module.packagename" value="org.jdesktop.wonderland.modules.microphone"/>
    <property name="module.jarname" value="microphone"/>

    <pathconvert property="module-server.classpath">
        <path location="${current.dir}/../../tools/audio-manager/lib/voicelib.jar"/>
        <path location="${current.dir}/../../tools/audio-manager/lib/voip.jar"/>
        <path location="${current.dir}/../../tools/audio-manager/lib/bridge_connector.jar"/>
        <path location="${current.dir}/../../tools/audio-manager/build/lib/server/audiomanager-server.jar"/>
        <path location="${current.dir}/../../tools/orb/build/lib/server/orb-server.jar"/>
    </pathconvert>

    <!-- microphone is dependent on the jmecolladaloader -->
    <pathconvert property="module-client.classpath">
        <path location="${current.dir}/../jmecolladaloader/build/classes"/>
        <path location="${current.dir}/../../tools/audio-manager/build/lib/client/audiomanager-client.jar"/>
    </pathconvert>


    <!-- import common build file -->
    <import file="${modules.dir}/build-tools/build-scripts/module-build.xml"/>
       
    <target name="build" depends="-module-init, -module-compile-common,
                                                -module-compile-server,
                                                -module-compile-client"/>
   
    <target name="dist" depends="build">
        <mkdir dir="${module.dist.dir}"/>
        
        <module name="microphone" version="1" minorversion="1"
                jarfile="${module.dist.dir}/${module.jarname}.jar"
                builddir="${build.lib.dir}">
            <requires name="audiomanager" version="1"/>
            <client>
                <clientjar name="microphone-client" basedir="${build.classes.dir}">
                    <include name="${module.src}/client/**"/>
                    <include name="${module.src}/common/**"/>
                    <fileset dir="${current.dir}/src/classes">
                        <include name="${module.src}/client/cell/resources/**/*.png"/>
                    </fileset>
                </clientjar>
            </client>
                
            <server>
                <serverjar name="microphone-server" basedir="${build.classes.dir}">
                    <include name="${module.src}/server/**"/>
                    <include name="${module.src}/common/**"/>
                </serverjar>

            </server>
            <art dir="${current.dir}/art"/>
        </module>
    </target>
                                           
    <target name="deploy" depends="dist, -module-deploy"/>

    <target name="clean" depends="-module-clean"/>
</project>
