<?xml version="1.0" encoding="UTF-8"?>
<project name="jeditortest" default="dist" basedir=".">
    <!-- current directory needed in all scripts -->
    <dirname property="current.dir" file="${ant.file.jeditortest}"/>
    <property name="modules.dir" location="${current.dir}/../.."/>
        
    <property name="module.src" value="org/jdesktop/wonderland/modules/jeditortest"/>
    <property name="module.packagename" value="org.jdesktop.wonderland.modules.jeditortest"/>
    <property name="module.jarname" value="jeditortest"/>
    
    <property name="module-common.classpath" value="${current.dir}/../appbase/build/client/appbase-client.jar"/>
    <property name="module-client.classpath" value="${current.dir}/../appbase/build/client/appbase-client.jar"/>
    <property name="module-server.classpath" value="${current.dir}/../appbase/build/server/appbase-server.jar"/>

    <!-- import common build file -->
    <import file="${modules.dir}/build-tools/build-scripts/module-build.xml"/>
       
    <target name="build" depends="-module-init, -module-compile-common, 
                                                -module-compile-server,
                                                -module-compile-client"/>
    <target name="dist" depends="build">
        <mkdir dir="${module.dist.dir}"/>
        
        <module name="jeditortest" version="1" jarfile="${module.dist.dir}/${module.jarname}.jar"
                builddir="${build.dir}">
                
            <client>
                <clientjar name="jeditortest-client" basedir="${build.classes.dir}">
                    <include name="${module.src}/client/**"/>
                    <include name="${module.src}/common/**"/>
		    <!-- include resources -->
		    <fileset dir="${javasrc.dir}">
		      <include name="${module.src}/client/resources/**"/>
		    </fileset>
                </clientjar>
            </client>
                
            <server>
                <serverjar name="jeditortest-server" basedir="${build.classes.dir}">
                    <cellsetup>
                        <provider classname="org.jdesktop.wonderland.modules.jeditortest.server.JEditorTestCellServerState"/>
                    </cellsetup>
                    <include name="${module.src}/server/**"/>
                    <include name="${module.src}/common/**"/>
                </serverjar>
            </server>
            
            <art dir="${current.dir}/art"/>
        </module>
    </target>                                                       
                                           
    <target name="deploy" depends="dist, -module-deploy"/>

    <target name="clean" depends="-module-clean"/>
</project>
