<?xml version="1.0" encoding="UTF-8"?>
<project name="placemarks" default="dist" basedir=".">
    <!-- current directory needed in all scripts -->
    <dirname property="current.dir" file="${ant.file.placemarks}"/>
    <property name="modules.dir" location="${current.dir}/../.."/>
        
    <property name="module.src" value="org/jdesktop/wonderland/modules/placemarks"/>
    <property name="module.packagename" value="org.jdesktop.wonderland.modules.placemarks"/>
    <property name="module.jarname" value="placemarks"/>
    
    <!-- import common build file -->
    <import file="${modules.dir}/build-tools/build-scripts/module-build.xml"/>

    <!-- import web build file -->
    <import file="${web.dir}/build-tools/build-scripts/web-build.xml"/>

    <!-- pathconvert property="module-common.classpath">
    </pathconvert -->

    <pathconvert property="module-client.classpath">
        <path location="${modules.dir}/foundation/content-repository/build/client/contentrepo-client.jar"/>
    </pathconvert>

    <pathconvert property="module-web.classpath">
        <path location="${modules.dir}/foundation/content-repository/build/lib/contentrepo-spi.jar"/>
        <path location="${modules.dir}/foundation/content-repository/build/client/contentrepo-client.jar"/>
        <path location="${web.dir}/front/dist/wonderland-webadmin-core.jar"/>
        <path location="${modules.dir}/tools/darkstar/build/weblib/darkstar-runner.jar"/>
        <path location="${web.dir}/runner/dist/wonderland-runner-api.jar"/>
        <path location="${core.dir}/build/lib/wonderland-client.jar"/>
        <path location="${core.dir}/build/lib/wonderland-common.jar"/>
    </pathconvert>
       
    <target name="build" depends="-module-init, -module-compile-common, 
                                                -module-compile-server,
                                                -module-compile-client,
						                        -placemarks-compile-web"/>

    <target name="-placemarks-compile-web" depends="-module-init, -module-compile-common">
        <wl-compile>
            <compile-classes>
                <include name="${module.src}/web/**"/>
            </compile-classes>

            <compile-classpath>
                <pathelement path="${web.classpath}"/>
                <pathelement path="${module-web.classpath}"/>
            </compile-classpath>
        </wl-compile>
    </target>

    <target name="dist" depends="build">
        <mkdir dir="${module.dist.dir}"/>
        
        <module name="placemarks" version="1" jarfile="${module.dist.dir}/${module.jarname}.jar"
                builddir="${build.dir}">

          <requires name="content-repository" version="1"/>
          <requires name="darkstar" version="1"/>

           <client>
             <clientjar name="placemarks-client" basedir="${build.classes.dir}">
               <include name="${module.src}/client/**"/>
               <include name="${module.src}/common/**"/>
             </clientjar>
           </client> 

          <server>
                <serverjar name="placemarks-server" basedir="${build.classes.dir}">
                    <include name="${module.src}/server/**"/>
                    <include name="${module.src}/common/**"/>
                </serverjar>
           </server>

            <part name="web">
                <jar name="wonderland-placemarks.war">
                    <zipfileset dir="${build.classes.dir}" prefix="WEB-INF/classes">
                        <include name="${module.src}/web/**"/>
                        <include name="${module.src}/common/**"/>
                    </zipfileset>

                    <fileset dir="${current.dir}/web"/>
                </jar>
            </part>            
        </module>
    </target>                                                       
                                           
    <target name="deploy" depends="dist, -module-deploy"/>
    <target name="clean" depends="-module-clean"/>
</project>
