<?xml version="1.0" encoding="UTF-8"?>
<project name="wonderland-selection-module" default="all" basedir=".">
    <!-- current directory needed in all scripts -->
    <dirname property="current.dir" file="${ant.file.wonderland-selection-module}"/>
    <property name="modules.dir" location="${current.dir}/../.."/>
        
    <property name="module.src" value="org/jdesktop/wonderland/modules/selection"/>
    <property name="module.packagename" value="org.jdesktop.wonderland.modules.selection"/>
    <property name="module.jarname" value="selection-module"/>
    
    <!-- import common build file -->
    <import file="${modules.dir}/build-tools/build-scripts/module-build.xml"/>
       
    <target name="build" depends="-module-init, -module-compile-common, 
                                                -module-compile-server,
                                                -module-compile-client"/>
    
    <target name="dist" depends="build">
        <mkdir dir="${module.dist.dir}"/>
        
        <module name="selection" version="1" jarfile="${module.dist.dir}/${module.jarname}.jar"
                builddir="${build.dir}">
                    
            <client>
                <clientjar name="selection-client" basedir="${build.classes.dir}">
                    <include name="${module.src}/client/**"/>
                </clientjar> 
            </client>
        </module>
    </target>                                  
             
    <target name="deploy" depends="dist, -module-deploy"/>
    <target name="clean" depends="-module-clean"/>
</project>
